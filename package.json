{
  "name": "ecommerce",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build",
    "start:server": "lerna run start:server:dev",
    "start:web": "lerna run start:web",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "version": "independent"
  },
  "devDependencies": {
    "lerna": "^8.1.3"
  }
}
